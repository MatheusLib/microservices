name: deploy-audit-service

on:
  push:
    branches: ["main"]
    paths:
      - "microservices/audit-service/**"
      - "microservices/deploy/systemd/audit-service.service"

jobs:
  deploy_audit:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy audit-service to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            if [ ! -d "$HOME/microservices/.git" ]; then
              git clone https://github.com/${{ github.repository }} "$HOME/microservices"
            fi
            cd "$HOME/microservices"
            git pull --ff-only
            sudo cp "$HOME/microservices/microservices/deploy/systemd/audit-service.service" /etc/systemd/system/audit-service.service
            sudo systemctl daemon-reload
            sudo systemctl restart audit-service
